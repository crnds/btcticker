<?xml version="1.0" encoding="utf-8"?>
<!-- Static preview shown in the widget picker (previewLayout, API 31+). -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:padding="10dp">

    <ImageButton
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@null"
        android:src="@drawable/ic_widget_refresh"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:contentDescription="@string/widget_refresh" />

    <!-- Fixed size that fills the height without needing live measurement -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="104,888"
        android:textColor="#e0e0e0"
        android:fontFamily="sans-serif-condensed"
        android:textStyle="bold"
        android:textSize="38sp"
        android:maxLines="1"
        android:gravity="start|center_vertical" />

    <TextView
        android:id="@+id/preview_change"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:text="+6%"
        android:textColor="#00e676"
        android:textSize="11sp"
        android:fontFamily="monospace" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toStartOf="@id/preview_change"
        android:layout_marginEnd="5dp"
        android:text="just now"
        android:textColor="#444444"
        android:textSize="9sp"
        android:fontFamily="monospace" />

</RelativeLayout>
